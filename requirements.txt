# Core library has zero runtime dependencies (pure standard library).
# All functionality works with only the Python standard library.
#
# To install development and testing tools:
#   pip install -r requirements-dev.txt
